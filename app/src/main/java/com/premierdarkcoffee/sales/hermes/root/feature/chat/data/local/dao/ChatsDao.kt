package com.premierdarkcoffee.sales.hermes.root.feature.chat.data.local.dao

/*
import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import com.premierdarkcoffee.sales.hermes.root.feature.chat.data.local.entity.chat.ChatEntity

@Dao
interface ChatsDao {

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertChatMessage(chatMessage: ChatEntity)

    @Query("SELECT * FROM client_chats ORDER BY date")
    suspend fun getAllChatMessages(): List<ChatEntity>

}
*/
