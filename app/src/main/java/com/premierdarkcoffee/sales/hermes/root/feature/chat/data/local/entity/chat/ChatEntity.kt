package com.premierdarkcoffee.sales.hermes.root.feature.chat.data.local.entity.chat

/*
import androidx.room.Entity
import androidx.room.PrimaryKey
import com.premierdarkcoffee.sales.hermes.root.feature.chat.domain.model.chat.Chat

@Entity(tableName = "client_chats")
data class ChatEntity(@PrimaryKey(autoGenerate = false) val id: String,
                      val title: String,
                      val messages: List<ChatMessageEntity>,
                      val date: Long) {

    fun toChat(): Chat {
        return Chat(id, title, messages.map { it.toChatMessage() }, date)
    }
}
*/
