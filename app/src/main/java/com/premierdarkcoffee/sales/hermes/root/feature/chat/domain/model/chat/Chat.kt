package com.premierdarkcoffee.sales.hermes.root.feature.chat.domain.model.chat

/*
data class Chat(val id: String = UUID.randomUUID().toString(),
                val title: String,
                val messages: List<ChatMessage>,
                val date: Long = System.currentTimeMillis()) {

    fun toChatEntity(): ChatEntity {
        return ChatEntity(id, title, messages.map { it.toChatMessageEntity() }, date)
    }
}*/
